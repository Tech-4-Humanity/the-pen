{
  "fn": "enqueue_job",
  "action": "worker.bootstrap_mac_or_ec2",
  "idempotency_key": "worker-bootstrap-mac-011",
  "priority": 1,
  "payload": {
    "purpose": "ensure active worker exists to process sweeper jobs",
    "preferred_targets": [
      "mac_endpoint",
      "ec2_runner"
    ],
    "bootstrap_actions": [
      "check if worker process is alive",
      "if not, start worker runtime",
      "verify GitHub polling for inbox/",
      "verify ability to write receipts/runtime/",
      "emit heartbeat log"
    ],
    "required_outputs": {
      "receipt_path_prefix": "receipts/runtime/worker-bootstrap-mac-011",
      "artifacts": [
        "worker_status.json",
        "heartbeat.log",
        "bootstrap_summary.md"
      ]
    },
    "definition_of_done": [
      "worker process confirmed running",
      "inbox polling active",
      "receipt write capability confirmed",
      "heartbeat emitted"
    ]
  },
  "_repair": {
    "reason": "missing required fields",
    "missing_fields": [
      "origin",
      "destination",
      "topic"
    ],
    "quarantined_at": "2026-04-29T00:13:34Z",
    "source_file": "worker-bootstrap-mac-011.json"
  }
}
