name: Bridge Runner

on:
  push:
    paths:
      - 'bridge/payloads/**.json'
  pull_request:
    paths:
      - 'bridge/payloads/**.json'
      - '.github/workflows/bridge-runner.yml'
  workflow_dispatch:
    inputs:
      payload_path:
        description: 'Specific payload file to run (optional)'
        required: false

permissions:
  contents: write

concurrency:
  group: bridge-runner-${{ github.ref }}
  cancel-in-progress: false

jobs:
  bridge-runner:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 2
          persist-credentials: true

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: PR-safe acknowledgement
        if: github.event_name == 'pull_request'
        run: |
          echo "Bridge Runner observed payload/workflow change in PR context."
          echo "Write-capable bridge execution and receipt commits are disabled for pull_request."
          echo "classification=PR_SAFE_NO_OP"

      - name: Detect changed payloads
        if: github.event_name != 'pull_request'
        id: detect
        env:
          INPUT_PATH: ${{ github.event.inputs.payload_path || '' }}
        run: |
          set -euo pipefail
          if [ -n "${INPUT_PATH:-}" ]; then
            echo "files=${INPUT_PATH}" >> "$GITHUB_OUTPUT"
          else
            CHANGED=$(git diff --name-only HEAD~1 HEAD -- 'bridge/payloads/*.json' 2>/dev/null | tr '\n' ' ' || true)
            echo "files=${CHANGED:-}" >> "$GITHUB_OUTPUT"
          fi

      - name: Execute payloads
        if: github.event_name != 'pull_request'
        env:
          BRIDGE_ENDPOINT: ${{ secrets.BRIDGE_ENDPOINT }}
          BRIDGE_API_KEY:  ${{ secrets.BRIDGE_API_KEY || secrets.BRIDGE_KEY }}
          SUPABASE_URL:    ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          GH_SHA:    ${{ github.sha }}
          GH_RUN_ID: ${{ github.run_id }}
          FILES_TO_RUN: ${{ steps.detect.outputs.files }}
        run: |
          set -uo pipefail
          if [ -z "${FILES_TO_RUN// }" ]; then
            echo "No payload files changed — nothing to execute"
            exit 0
          fi

          for PAYLOAD_PATH in $FILES_TO_RUN; do
            echo "=== Processing: $PAYLOAD_PATH ==="
            if [ ! -f "$PAYLOAD_PATH" ]; then
              echo "File not found: $PAYLOAD_PATH — skipping"
              continue
            fi

            REQUEST_ID=$(node -e 'const j=JSON.parse(require("fs").readFileSync(process.argv[1],"utf8")); console.log(j.metadata?.request_id || j.request_id || j.idempotency_key || "unknown-"+Date.now())' "$PAYLOAD_PATH")
            FUNCTION_NAME=$(node -e 'const j=JSON.parse(require("fs").readFileSync(process.argv[1],"utf8")); console.log(j.function_name || j.action || j.fn || "unknown")' "$PAYLOAD_PATH")

            echo "request_id=$REQUEST_ID function=$FUNCTION_NAME"

            mkdir -p bridge/receipts
            RECEIPT_FILE="bridge/receipts/${REQUEST_ID}.receipt.json"
            PICKED_UP_AT=$(date -u +"%Y-%m-%dT%H:%M:%SZ")

            INVOKE_STATUS="SKIPPED"
            INVOKE_RESPONSE='{}'
            HTTP_CODE=0

            if [ -n "${BRIDGE_ENDPOINT:-}" ] && [ -n "${BRIDGE_API_KEY:-}" ]; then
              HTTP_RESPONSE=$(curl -s -w '\n%{http_code}' -X POST "$BRIDGE_ENDPOINT" \
                -H 'Content-Type: application/json' \
                -H "x-api-key: $BRIDGE_API_KEY" \
                -d @"$PAYLOAD_PATH" --max-time 30 || printf '{"error":"curl_failed"}\n000')
              HTTP_CODE=$(printf '%s' "$HTTP_RESPONSE" | tail -n1)
              INVOKE_RESPONSE=$(printf '%s' "$HTTP_RESPONSE" | sed '$d')
              if [ "$HTTP_CODE" -ge 200 ] && [ "$HTTP_CODE" -lt 300 ]; then
                INVOKE_STATUS="REAL"
              else
                INVOKE_STATUS="PARTIAL"
              fi
            else
              INVOKE_STATUS="BLOCKED"
              INVOKE_RESPONSE='{"error":"BRIDGE_ENDPOINT or BRIDGE_API_KEY not configured"}'
            fi

            COMPLETED_AT=$(date -u +"%Y-%m-%dT%H:%M:%SZ")

            export REQUEST_ID FUNCTION_NAME PAYLOAD_PATH PICKED_UP_AT COMPLETED_AT INVOKE_STATUS HTTP_CODE INVOKE_RESPONSE RECEIPT_FILE
            node -e '
              const fs=require("fs");
              const e=process.env;
              const resp=(()=>{ try { return JSON.parse(e.INVOKE_RESPONSE); } catch { return { raw: e.INVOKE_RESPONSE }; } })();
              fs.writeFileSync(e.RECEIPT_FILE, JSON.stringify({
                request_id: e.REQUEST_ID, function_name: e.FUNCTION_NAME, payload_path: e.PAYLOAD_PATH,
                commit_sha: e.GH_SHA, runner_workflow: e.GH_RUN_ID,
                picked_up_at: e.PICKED_UP_AT, completed_at: e.COMPLETED_AT,
                status: e.INVOKE_STATUS, http_code: Number(e.HTTP_CODE), response: resp
              }, null, 2));
            '
            echo "Receipt: $RECEIPT_FILE | Status: $INVOKE_STATUS | HTTP: $HTTP_CODE"
          done

      - name: Commit and push receipts
        if: github.event_name != 'pull_request'
        run: |
          set -euo pipefail
          git config user.name  "bridge-runner[bot]"
          git config user.email "bridge-runner@tml-4pm"
          git add bridge/receipts/ || true
          if git diff --staged --quiet; then
            echo "No receipt changes to commit"
            exit 0
          fi
          TS=$(date -u +%Y%m%d-%H%M%S)
          git commit -m "chore: bridge receipts [skip ci] ${TS}"
          for attempt in 1 2 3 4 5; do
            if git pull --rebase --autostash origin "${GITHUB_REF_NAME}" && git push; then
              exit 0
            fi
            echo "push attempt ${attempt} rejected; retrying after rebase"
            sleep $((RANDOM % 8 + 2))
          done
          echo "::warning::receipt push failed after retries; execution receipt exists in runner workspace but was not committed"
