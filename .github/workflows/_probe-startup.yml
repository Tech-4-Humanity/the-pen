name: _Probe Startup (RETIRED)

# Diagnostic probe from 2026-05-30 the-pen CI triage. Purpose served:
# confirmed failing runs were on:pull_request context withholding (PR_CONTEXT_SECRET_WITHHELD),
# not runner/billing/secret failure. Neutered to stop emitting runs.
# Safe to delete from the GitHub UI. Has no triggers and runs nothing.

on:
  workflow_dispatch: {}

jobs:
  noop:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: 'echo retired'
