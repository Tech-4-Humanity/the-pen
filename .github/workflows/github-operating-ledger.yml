name: GitHub Operating Ledger

on:
  workflow_dispatch:

# INCIDENT CONTAINMENT:
# schedule disabled during cross-repo Actions bootstrap failure investigation.

permissions:
  contents: read

jobs:
  ledger:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Ledger canary
        run: echo github-operating-ledger-canary-ok
