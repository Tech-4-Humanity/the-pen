name: Deploy Say It Once

on:
  push:
    paths:
      - 'packs/say-it-once/**'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Install
        run: npm install
      - name: Build
        run: npm run build --prefix packs/say-it-once/vercel-demo
      - name: Deploy
        run: echo "Hook into Bridge Runner / Vercel deploy here"