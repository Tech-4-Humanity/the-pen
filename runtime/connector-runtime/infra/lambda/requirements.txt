# Lambda runtime dependencies for connector-probe-runner.
# boto3 is provided by the python3.12 Lambda runtime, but pinning here lets
# `sam build` produce a reproducible artifact and protects against runtime drift.
boto3==1.35.36
botocore==1.35.36
