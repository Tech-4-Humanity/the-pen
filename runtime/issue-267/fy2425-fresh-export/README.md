# Issue #267 — FY24/25 fresh MAAT export

This directory is the privacy-safe GitHub locator for the governed FY24/25 export.

Private bank transactions, source statements and reconciliation outputs are **not committed**. They are generated by `tools/fy2425_maat_fresh_export.py` in the governed Pen/AWS runtime and published to:

` s3://t4h-archive-140548542136/fy2425/non-labour/issue-267/ `

Required period: `2024-07-01 <= posted_at < 2025-07-01`.

Required accounts are identified only by institution and last four digits. The run also enumerates additional transfer, repayment, refund and loan reconciliation accounts.

A run is classified `REAL` only after all 13 required outputs exist, every object has a SHA-256, upload succeeds, each S3 object is independently downloaded, and readback SHA-256 matches.

Current repository state: execution tooling committed; runtime export and S3 proof remain pending until run in the authorised Pen/AWS execution plane with governed MAAT and AWS credentials.